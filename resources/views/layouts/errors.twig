{% if errors.count %}
    <div class="form-group">
        <div class="alert alert-danger">
            <ul>
                {% for error in errors.all() %}
                    <li>
                        {{ error }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endif %}
